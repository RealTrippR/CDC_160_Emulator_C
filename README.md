# CDC 160 Emulator
<BR>

This is a work in progress. It comes with an assembler and can build and run most programs, as long as they do not use the EXF instruction. Even though the external function (EXF) instruction is implemented and should work, it has not been fully tested.
